<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Shadow Layer -->
    <!-- <item>
        <shape android:shape="rectangle">
            <padding android:top="1dp"/>
             Adjust padding to control shadow spread
            <solid android:color="#10000000" />  Adjust alpha for shadow intensity
            <corners android:topLeftRadius="16dp" android:topRightRadius="16dp"/> Adjust radius if needed
        </shape>
    </item> -->

    <!-- Top Border Line -->
    <item android:top="0dp" android:left="-2dp" android:right="-2dp" android:bottom="-2dp">
        <shape android:shape="rectangle">
            <stroke android:width="1dp" android:color="#E0E0E0" /> <!-- Light grey border -->
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>

    <!-- Background Color -->
    <item android:top="1dp"> <!-- Offset slightly to show the border -->
        <shape android:shape="rectangle">
            <solid android:color="@color/bottom_nav_background" />
            <!-- Optional: Add top corner radius
            <corners android:topLeftRadius="16dp" android:topRightRadius="16dp"/>
            -->
        </shape>
    </item>

</layer-list>